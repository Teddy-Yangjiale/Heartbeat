name: heartbeat
channels:
  - defaults
dependencies:
  - python=3.11
  - numpy
  - scipy
  - pandas
  - matplotlib
  - pip
  - pip:
      - streamlit
      - fastapi
      - uvicorn
      - python-multipart
      - httpx
      - opencv-python
      - moviepy
      - pydub
      - librosa
      - soundfile
      - imageio-ffmpeg
